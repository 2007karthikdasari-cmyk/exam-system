<nav class="navbar navbar-expand-lg fixed-top">
    <div class="container">
        <a class="navbar-brand" href="/">
            <div class="brand-wrapper">
                <i class="fas fa-graduation-cap"></i>
                <div class="brand-text-wrapper">
                    <span class="brand-text">EXAM<span class="gold-text">SYSTEM</span></span>
                    <small class="tagline">Online Examination Portal</small>
                </div>
            </div>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <% if (user) { %>
                    <li class="nav-item">
                        <span class="nav-link">
                            <i class="fas fa-user-circle"></i> Welcome, <%= user.name %>
                        </span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/<%= user.role === 'admin' ? 'admin/dashboard' : 'user/dashboard' %>">
                            <i class="fas fa-tachometer-alt"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">
                            <i class="fas fa-sign-out-alt"></i> Logout
                        </a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="#home">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#features">Features</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#about">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#contact">Contact</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
<div style="height: 90px;"></div>